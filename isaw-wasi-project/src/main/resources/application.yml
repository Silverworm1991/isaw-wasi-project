spring:
  datasource:
    url: jdbc:mysql://localhost:3306/i_saw_was_i?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: isawuser
    password: isawpass
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
